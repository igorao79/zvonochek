# 🎙️ Аудиозвонки WebRTC на Next.js + Supabase

Приложение для реалтайм аудиозвонков, построенное на Next.js 15, TypeScript, Supabase и WebRTC.

## 🚀 Возможности

- ✅ Аутентификация пользователей через Supabase Auth
- ✅ **Реалтайм аудиозвонки** через WebRTC (без видео)
- ✅ Высокое качество звука с шумоподавлением
- ✅ Сигнализация в реальном времени через Supabase Realtime
- ✅ Управление микрофоном (вкл/выкл)
- ✅ Современный UI с градиентами и анимациями
- ✅ Полная типизация TypeScript
- ✅ Mobile-first дизайн

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Аккаунт Supabase

## 🛠️ Установка

### 1. Клонирование и установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ezwhnimiesqrhsuohgva.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_CpGdIh8Yht0hSDltKXHdyQ_VonVWaMy
```

### 3. Настройка базы данных Supabase

**Важно:** Выполните SQL скрипт для создания необходимых таблиц и политик.

#### Способ 1: Через Supabase Dashboard (рекомендуется)
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в раздел **SQL Editor**
4. Скопируйте содержимое файла `supabase-setup.sql` и выполните его
5. **Примечание:** Скрипт можно запускать повторно - он содержит проверки на существование объектов и удаляет дубликаты перед созданием

#### Проверка настройки базы данных
```bash
# Запустите скрипт проверки
node check-db.js
```

Этот скрипт проверит:
- ✅ Существование таблицы `profiles`
- ✅ Существование bucket `avatars`
- ✅ Корректность настроек

#### Способ 2: Через скрипт настройки
```bash
node setup-db.js
```
Этот скрипт покажет инструкции и содержимое SQL файла.

#### Что создаст скрипт:
- Таблицу `profiles` для хранения профилей пользователей
- RLS политики для безопасности
- Триггеры для автоматического создания профилей при регистрации
- Индексы для производительности

#### Проверка настройки:
После выполнения SQL скрипта приложение автоматически создаст профили для зарегистрированных пользователей.

### 4. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 3. Настройка Supabase

#### Включение Realtime

В вашем проекте Supabase необходимо включить Realtime:

1. Перейдите в Dashboard Supabase → Settings → API
2. Убедитесь, что Realtime включен
3. В разделе Database → Replication включите Realtime для нужных таблиц (опционально)

#### Настройка Email провайдера (для аутентификации)

1. Перейдите в Authentication → Providers
2. Включите Email провайдер
3. Настройте email templates по желанию

### Продакшн сборка

```bash
npm run build
npm start
```

## 📱 Как использовать

### 1. Регистрация

- Откройте приложение
- Нажмите "Нет аккаунта? Зарегистрироваться"
- Введите email и пароль
- Подтвердите email (проверьте почту)

### 2. Вход

- Введите email и пароль
- Нажмите "Войти"

### 3. Начать звонок

1. После входа вы увидите список всех пользователей
2. Найдите нужного пользователя в списке
3. Нажмите кнопку "Позвонить" рядом с пользователем
4. Или добавьте пользователя в контакты для быстрого доступа
5. Пользователь получит уведомление о входящем звонке
6. После принятия звонка установится аудиосвязь в реалтайм

### 4. Управление звонком

- Во время звонка вы можете:
  - **Выключить/включить микрофон** - кнопка с иконкой микрофона
  - **Завершить звонок** - красная кнопка
- Звук работает в обе стороны одновременно (full-duplex)
- Автоматическое шумоподавление и эхоподавление

### 5. Контакты

- Добавляйте часто используемых пользователей в контакты
- Контакты сохраняются в браузере
- Быстрый доступ к звонкам через список контактов

## 🏗️ Архитектура проекта

```
zvonokmini/
├── app/
│   ├── auth/
│   │   └── callback/       # OAuth callback обработчик
│   ├── login/              # Страница входа/регистрации
│   ├── page.tsx            # Главная страница с видеозвонками
│   ├── layout.tsx          # Root layout
│   └── globals.css         # Глобальные стили
├── lib/
│   ├── supabase/
│   │   ├── client.ts       # Supabase клиент (браузер)
│   │   ├── server.ts       # Supabase клиент (сервер)
│   │   └── middleware.ts   # Middleware для сессий
│   ├── types.ts            # TypeScript типы
│   └── webrtc.ts           # WebRTC сервис
├── middleware.ts           # Next.js middleware
└── .env.local             # Переменные окружения
```

## 🔧 Технологии

- **Next.js 15** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Supabase** - Backend (Auth, Realtime)
- **WebRTC** - Peer-to-peer видеосвязь
- **Tailwind CSS** - Стилизация
- **@supabase/ssr** - SSR поддержка для Supabase

## 🔒 Безопасность

- Аутентификация пользователей через Supabase Auth
- Защита маршрутов через middleware
- Cookie-based сессии
- HTTPS для продакшена (обязательно для WebRTC)

## 🌐 WebRTC сигнализация

Приложение использует Supabase Realtime для сигнализации WebRTC:

1. **Offer/Answer** - обмен SDP для установки соединения
2. **ICE Candidates** - обмен кандидатами для NAT traversal
3. **End Call** - сигнал завершения звонка

## 📊 Производительность

- Используются STUN серверы Google для NAT traversal
- Peer-to-peer соединение для минимальной задержки
- Оптимизация аудио кодеков через WebRTC API
- Буферизация сигналов при отсутствии готовности peer

## 🔍 Диагностика проблем

### Пользователи не отображаются:
1. **Проверьте выполнение SQL скрипта** `supabase-setup.sql` в Supabase Dashboard
2. **Откройте консоль браузера** (F12 → Console) и посмотрите логи API `/api/users`
3. **Нажмите "Создать мой профиль"** если профиль не создан автоматически
4. **Проверьте авторизацию** - убедитесь что вы вошли в систему

### Звонки не работают:
1. **Разрешите доступ к микрофону** в настройках браузера
2. **Проверьте HTTPS соединение** (WebRTC требует безопасного соединения)
3. **Откройте консоль браузера** для отладки WebRTC сигналов
4. **Убедитесь что оба пользователя онлайн** и имеют профили

### Соединение не устанавливается:
1. **Проверьте что оба браузера открыты** и авторизованы
2. **Посмотрите на статусы** - один должен показывать "Звоним...", другой "Входящий звонок!"
3. **Нажмите "Ответить"** на входящем звонке
4. **Проверьте логи** в консоли браузера для сигналов WebRTC
5. **Обновите страницу** если возникли ошибки состояния WebRTC

### Ошибка "Called in wrong state":
- **Перезагрузите браузер** для обоих пользователей
- **Убедитесь** что нет других активных соединений
- **Проверьте** что используется HTTPS (для WebRTC требуется безопасность)

### Таблица profiles не существует:
- Выполните SQL скрипт `supabase-setup.sql` в разделе SQL Editor Supabase Dashboard
- Или запустите `node setup-db.js` для просмотра инструкций

## 🐛 Возможные проблемы

### Камера/микрофон не работают

- Убедитесь, что браузер имеет разрешения на камеру и микрофон
- Используйте HTTPS (WebRTC требует безопасное соединение)
- В режиме разработки localhost считается безопасным

### Не удается установить соединение

- Проверьте настройки firewall
- Убедитесь, что Realtime включен в Supabase
- Проверьте консоль браузера на ошибки

### Email подтверждение не приходит

- Проверьте папку спам
- Настройте SMTP в Supabase (Settings → Auth → SMTP)

## 📝 Дальнейшие улучшения

- [ ] Групповые звонки
- [ ] Запись звонков
- [ ] Текстовый чат во время звонка
- [ ] Фильтры и эффекты для видео
- [ ] Демонстрация экрана
- [ ] Список контактов
- [ ] История звонков
- [ ] TURN сервер для сложных NAT

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с помощью Claude AI для демонстрации возможностей WebRTC + Supabase
